================================================================================
  PMO DASHBOARD — HORIZONTES ARQUITETURA
  Documentação completa para recriar o projeto do zero
================================================================================

Esse documento contém TUDO que foi descoberto durante a integração do dashboard
com o Microsoft Project Online (PWA). Use isso como referência única se for
recriar o projeto.


================================================================================
  1. OBJETIVO E CONTEXTO
================================================================================

Dashboard web em Flask + HTML que mostra dados dos projetos do PWA da Horizontes.
Origem dos dados: Microsoft Project Online (PWA / SharePoint Online).
Autenticação: OAuth2 via MSAL (suporta MFA).
Idioma: pt-BR.


================================================================================
  2. INFRAESTRUTURA E CREDENCIAIS
================================================================================

PWA URL:
    https://horizontesarq.sharepoint.com/sites/pwa

Tenant Azure AD:
    Tenant ID: c4cae996-6b0f-4e97-86b8-3f59107fe51c

App Registration no Azure AD:
    Client ID: 23bdfa12-e9f6-4759-90bf-e6d65bd5fd09
    Tipo:      Public client (sem secret)
    Auth:      Device code flow (suporta MFA)
    Redirect:  Mobile and desktop applications
    Permissão: SharePoint > AllSites.Read (delegated)

Conta de testes:
    pedrohenrique@horizontesarquitetura.com.br

Grupos SharePoint do PWA em que a conta está:
    [11] Administradores para o Project Web App
    [8]  Visitantes da Project Web App
    [13] Visualizadores de portfólio para o Project Web App


================================================================================
  3. ARMADILHA CRÍTICA DE PERMISSÕES (importante!)
================================================================================

Este tenant tem a configuração de segurança interna do PWA QUEBRADA:

  - A UI de admin "PWA Settings > Security > Manage Groups" retorna HTTP 500
  - Os grupos SharePoint ("Administradores para o Project Web App" etc.) NÃO
    estão vinculados aos grupos de segurança internos do Project Server
  - Resultado: mesmo um admin SharePoint só recebe duas permissões PWA:
        Projects.Add  e  Resources.Add
  - PSI SOAP (/_vti_bin/PSI/*) responde 401 "Attempted unauthorized operation"
  - /_api/ProjectData/* (OData reporting) responde 403 GeneralSecurityAccessDenied
  - /_api/ProjectServer/Projects('id')/Tasks responde 404 "This project either
    does not exist..."

O que NÃO funciona neste tenant:
  ✗ /_api/ProjectData/Tasks                  → 403
  ✗ /_api/ProjectData/Assignments            → 403
  ✗ /_api/ProjectData/Resources              → 403
  ✗ /_api/ProjectServer/Projects(id)/Tasks   → 404
  ✗ /_api/ProjectServer/Projects(id)/Draft   → 404 (subresources)
  ✗ /_api/ProjectServer/ProjectResources     → 403
  ✗ /_api/ProjectServer/SecurityGroups       → 404
  ✗ /_vti_bin/PSI/*.asmx                     → 401
  ✗ PWA admin .aspx pages                    → 401 (precisam de cookie)


================================================================================
  4. A DESCOBERTA QUE DESTRAVOU TUDO
================================================================================

Embora /Projects(id)/Tasks retorne 404, fazer um filter+expand na LISTA
contorna a checagem de permissão e devolve as tarefas:

   FUNCIONA:
   GET /_api/ProjectServer/Projects?
       $filter=Id eq guid'<PROJECT_ID>'
       &$expand=Tasks,Tasks/Assignments,Tasks/Assignments/Resource

   NÃO funciona:
   GET /_api/ProjectServer/Projects('<PROJECT_ID>')/Tasks

Esse mesmo padrão (filter+expand na lista) destravou:
  - Tasks
  - Assignments
  - Custom field values (via Draft/IncludeCustomFields)
  - Resource names (via Tasks/Assignments/Resource)


================================================================================
  5. LIMITES DA API DESCOBERTOS
================================================================================

5.1 — $expand com sub-coleções: limite de 20 projetos por request.
      Acima disso retorna 500 "Too many projects: 27. You cannot load
      dependent objects for more than 20 projects."
      SOLUÇÃO: batches de 15 projetos com filter OR.

5.2 — $select dentro de $expand: NÃO SUPORTADO.
      Tentativa: $expand=Tasks($select=Id,Name) → HTTP 400
      Tem que aceitar TODOS os campos do expand.

5.3 — $filter aninhado em coleções: NÃO SUPORTADO.
      Tentativa: $filter=CustomFields/LookupEntries/Id eq guid'...' → HTTP 400

5.4 — _get_all sem $top falha em coleções com expand.
      Sempre passe $top=200 (ou maior que o total esperado).

5.5 — A pagina raiz /_api/ProjectServer (sem ID) FUNCIONA e lista todas
      as coleções disponíveis no servidor.


================================================================================
  6. AUTENTICAÇÃO MSAL (device flow)
================================================================================

Scope:    ["https://horizontesarq.sharepoint.com/AllSites.Read"]
Authority: https://login.microsoftonline.com/<TENANT_ID>

Fluxo:
  1. app.initiate_device_flow(scopes=SCOPES)
     → retorna { user_code, verification_uri }
  2. Usuário abre https://login.microsoft.com/device e digita o código
  3. Faz login normal (com MFA se exigido)
  4. app.acquire_token_by_device_flow(flow) devolve o token

O token vem com:
  - access_token (válido por ~1h)
  - refresh_token (válido por 90 dias)

A biblioteca MSAL cuida da renovação silenciosa.
O cache fica em .token_cache.json (NÃO COMMITAR).

Importante: após mudança de grupo de segurança no SharePoint, o token
antigo ainda reflete a membership antiga. Tem que apagar o cache e
re-autenticar (pwa_client.logout() + start_device_flow()).


================================================================================
  7. ESTRUTURA DA RESPOSTA DE /Projects
================================================================================

GET /_api/ProjectServer/Projects?$top=1

Devolve um JSON em OData v3 (não v4):
{
  "d": {
    "results": [
      {
        "__metadata": { ... },
        "Id": "aaa8ed5d-24e3-ef11-9651-14755b998b8f",
        "Name": "0724 - Direcional Santa Amélia",
        "StartDate": "2025-02-05T06:00:00",
        "FinishDate": "2025-05-02T18:00:00",
        "PercentComplete": 0,
        ...
        "ProjectSummaryTask":  {"__deferred": ...},
        "Draft":               {"__deferred": ...},
        "Tasks":               {"__deferred": ...},
        "Assignments":         {"__deferred": ...},
        ...
      }
    ]
  }
}

Cada link "__deferred" é uma sub-coleção que pode ser expandida com $expand.


================================================================================
  8. EXTRAÇÃO DE CUSTOM FIELDS DO PROJETO
================================================================================

Via $expand=Draft/IncludeCustomFields, o servidor adiciona ao projeto
propriedades nomeadas como:

    Custom_x005f_<cf_id_sem_hifens>

Para custom field SCALAR (FieldType=15, número/texto):
    Custom_x005f_0e73e9fa58d2ee11bc6b00155de08238 = "724.00000"

Para custom field LOOKUP (FieldType=21):
    Custom_x005f_bf41a3bdbbd1ee118cb900155de49236 = {
      "__metadata": { "type": "Collection(Edm.String)" },
      "results":    [ "Entry_b696dba5bbd1ee11b61a00155de44640" ]
    }

Para resolver Entry_xxxx em texto legível ("Belo Horizonte" etc.), tem
que cruzar com a LookupTable correspondente:

    GET /_api/ProjectServer/LookupTables?$expand=Entries

Cada Entry tem:
    Id, FullValue, InternalName ("Entry_" + Id sem hifens)


CUSTOM FIELDS DESCOBERTOS (entity=Project):

  Cidade        → bf41a3bd-bbd1-ee11-8cb9-00155de49236  (lookup)
  Cliente       → 4bb21a18-69e1-ee11-b3a1-00155de44340  (lookup)
  Coordenador   → 58ddf97d-9bd4-ee11-a15a-00155de04e43  (lookup)
  Número H.     → 0e73e9fa-58d2-ee11-bc6b-00155de08238  (número)
  Status        → d51e0c2e-42d2-ee11-ab3a-00155de43a39  (lookup)
  Tipo Projeto  → 02ecf97c-26e2-ee11-9730-00155de09043  (lookup)


LOOKUP TABLES PRINCIPAIS:

  Cidade        → b596dba5-bbd1-ee11-b61a-00155de44640  (6 entradas)
  Cliente       → 0c9d5e8c-68e1-ee11-b3a1-00155de44340  (7 entradas)
  Coordenador   → 7b6b2323-40d2-ee11-a725-00155de0463a  (8 entradas)
  Status        → ab3e65b8-41d2-ee11-8957-00155de4433a  (4 entradas)


CLIENTES (valores possíveis):
  Direcional Engenharia S/A
  Saint Gobain
  Sudecap
  Unops
  Vale
  Horizontes Arquitetura e Urbanismo
  Integra

CIDADES (valores possíveis):
  Belo Horizonte, Brumadinho, Mário Campos, Ourilândia,
  Poços de Caldas, São Paulo

COORDENADORES (valores possíveis):
  Cris Coutinho, Gabriel Velloso, Iris Dias, Marcelo Palhares Santiago,
  Mari Carneiro, Matheus Carvalho, Pedro Henrique Fonseca e Silva,
  Silvia Guastaferro


================================================================================
  9. CAMPOS DE TAREFA DISPONÍVEIS VIA $expand=Tasks
================================================================================

Ao expandir Tasks na lista de projetos, cada task vem com ~150 campos.
Os úteis para nós:

  Id, Name                                    → identificação
  OutlineLevel, OutlinePosition               → hierarquia
  IsSummary, IsMilestone, IsProjectSummary    → tipo
  Start, Finish                               → datas reais (ScheduledStart/Finish)
  ActualStart, ActualFinish                   → datas executadas
  BaselineStart, BaselineFinish               → linha de base
  Duration                                    → "57d" (string formatada)
  DurationMilliseconds                        → 1641600000 (mais preciso)
  BaselineDurationMilliseconds                → idem para BL
  PercentComplete                             → 0..100
  Work, ActualWork, RemainingWork             → horas (string "1.080h")
  WorkBreakdownStructure                      → WBS "1.2.3"

Para converter DurationMilliseconds em dias úteis: ms / (480*60*1000)
(480 minutos = 8h de trabalho por dia padrão MSP).

Para dias corridos (calendário): diferença em dias entre Start e Finish.


================================================================================
  10. NOMES DOS RECURSOS POR TAREFA
================================================================================

Não precisa fazer fetch separado de ProjectResources (que dá 403).
Usar nested expand de 3 níveis:

  $expand=Tasks,Tasks/Assignments,Tasks/Assignments/Resource

Cada Task vem com Assignments[].Resource.Name preenchido.

Aviso: a Task SUMMARY (a primeira, OutlineLevel=1) tem 0 assignments.
Os recursos aparecem só nas tasks folha (não-summary).


================================================================================
  11. % PREVISTO
================================================================================

Não é exposto pela API. CALCULADO por fórmula clássica MS Project:

  ref_start = baseline_start OR actual_start OR project_start
  ref_end   = baseline_end   OR actual_end   OR project_end

  Se hoje < ref_start: 0%
  Se hoje > ref_end:   100%
  Senão: (hoje - ref_start) / (ref_end - ref_start) * 100


================================================================================
  12. DIMENSÕES FINAIS USADAS NO DASHBOARD
================================================================================

PROJETO (8 campos):
  Nome, Cliente, Número Horizontes, Coordenador, Cidade,
  % Concluída (Project.PercentComplete),
  % Previsto (calculado),
  Última Publicação (LastPublishedDate → "ultimaPublicacao")

TAREFA (10 campos):
  Nome, Nomes dos Recursos, Duração (dias úteis), Dias Corridos,
  Início, Início da Linha de Base, Término, Término da Linha de Base,
  Nível da Estrutura de Tópicos,
  Crítico (IsCritical → "critical": bool)


================================================================================
  13. ESTRUTURA DE ARQUIVOS DO PROJETO
================================================================================

C:\Apps Python\Repos\Python\Dashboard\
├── pwa_client.py        — Cliente HTTP, MSAL auth, fetch_projects, fetch_tasks
├── app.py               — Flask: rotas /api/projects, /api/tasks/<id>, /api/auth/*
├── index.html           — Frontend (Chart.js + Leaflet)
├── fetcher.py           — Job batch que roda 3x/dia e gera data/*.json
├── report_base.py       — O cronograma como estava no último report gerado
├── export_campos.py     — Dump dos custom fields do PWA para planilha
├── comparador/
│   ├── comparador.py    — Motor: pareamento, variações, relatório em Markdown
│   └── rede.py          — Causalidade pela rede de dependências
├── report_semanal/
│   └── Report.py        — Monta o report semanal em Markdown
├── saude/
│   └── saude.py         — Analise de saude estrutural dos cronogramas (KPIs + score)
├── cronograma_alocacao/ — Gantts de projetos, equipe interna e fornecedores
├── desembolso/          — Curva de desembolso
├── ferias/              — Controle de férias
├── data/
│   ├── projects.json    — Snapshot de todos os projetos
│   ├── tasks_<pid>.json — Snapshot das tarefas por projeto
│   ├── report_base_<pid>.json — Cronograma do último report (id, start, end)
│   ├── fetch_state.json — Publicação já coletada com sucesso, por projeto
│   ├── lookup_cache.json— Cache das tabelas de pesquisa (validade de 7 dias)
│   ├── last_update.json — Timestamp do último fetch + status
│   └── fetch_progress.json — Andamento do run em curso (barra do botão Atualizar)
├── config.py            — Flags estáticas (ainda quase vazio)
├── requirements.txt     — msal, requests, flask, flask_cors
├── .token_cache.json    — Cache MSAL (NÃO COMMITAR)
├── .gitignore           — data/ inteira fica de fora do git
├── setup_scheduler.ps1  — Script para criar a tarefa no Windows Task Scheduler
└── PROJETO.txt          — Este arquivo


================================================================================
  14. PIPELINE AUTOMATIZADO (Task Scheduler)
================================================================================

Esquema:

   ┌──────────────────────────────┐
   │ Windows Task Scheduler       │ ← configurado uma vez via setup_scheduler.ps1
   │ Roda 3x/dia (09:15, 14:15, 16:45) │
   │ Se PC desligado nesses horarios:  │
   │   - StartWhenAvailable=true        │
   │   - roda quando o PC ligar         │
   └──────────┬───────────────────┘
              ↓
   ┌──────────────────────────────┐
   │ python fetcher.py            │
   │ - autentica via cache MSAL   │
   │ - chama PWA API              │
   │ - escreve data/*.json        │
   │ - escreve last_update.json   │
   │ - escreve fetch_progress.json│
   └──────────┬───────────────────┘

BARRA DE PROGRESSO DO BOTÃO "ATUALIZAR"
---------------------------------------
O fetcher publica o andamento em data/fetch_progress.json a cada avanço, e o
dashboard desenha uma faixa de 3 px sob o botão.

  {run_id, fase, rotulo, feito, total, ativo, atualizado_em}

  - fase: iniciando → autenticando → projetos → tarefas → limpeza → concluido
          (ou erro). Só a fase `tarefas` tem denominador: o número de projetos
          a coletar. Nas outras, total = 0 e a barra fica indeterminada.
  - O denominador são os projetos A COLETAR, não todos: os reaproveitados do
    snapshot entram como nota no rótulo. Barra sobre o trabalho que existe.
  - run_id: POST /api/refresh gera o identificador, grava o andamento inicial
    (o processo demora a subir) e passa `--run-id` ao fetcher. O browser só
    aceita andamento com esse id — sobra de coleta anterior é ignorada.
  - GET /api/progress devolve o arquivo. O browser pesquisa a cada 1 s e desiste
    se nada mudar por 3 min (fetcher travado ou morto).
  - Andamento é acessório: falha ao gravá-lo nunca interrompe a coleta.

COLETA INCREMENTAL
------------------
A lista de projetos é sempre atualizada (é barata e traz o LastPublishedDate de
todos). As tarefas — o grosso do tempo — só são buscadas nos projetos
republicados desde a última coleta bem-sucedida.

  - O carimbo da comparação é `publicadoEm` (LastPublishedDate COM hora, para
    distinguir duas publicações no mesmo dia). `ultimaPublicacao`, só data,
    continua sendo o campo exibido na tela.
  - data/fetch_state.json guarda a publicação EFETIVAMENTE coletada de cada
    projeto. É separado do projects.json de propósito: aquele é sobrescrito a
    cada run mesmo quando a coleta das tarefas falha; o estado só avança quando
    o tasks_<pid>.json foi gravado, então falha é retentada no run seguinte.
  - Projeto sem carimbo de publicação é sempre coletado (na dúvida, coleta).
  - `python fetcher.py --full` força a recoleta de todos.

Duas outras economias no mesmo caminho:

  - fetch_tasks usa $select com os 11 campos realmente consumidos. Sem ele o
    servidor devolvia a entidade inteira de cada tarefa e de cada atribuição.
    Se o servidor recusar os caminhos aninhados (HTTP 400/404/500), o cliente
    registra um WARNING e segue sem $select pelo resto do processo.
  - As tabelas de pesquisa (Cliente/Cidade/Coordenador) vão para
    data/lookup_cache.json com validade de 7 dias, em vez de serem baixadas a
    cada processo. Entry desconhecida força uma recarga, uma vez por processo.

              ↓
   ┌──────────────────────────────┐
   │ data/projects.json           │
   │ data/tasks_<pid>.json        │ ← lidos pelo Flask
   └──────────┬───────────────────┘
              ↓
   ┌──────────────────────────────┐
   │ app.py serve da memória/disco │
   │ Frontend abre instantâneo    │
   └──────────────────────────────┘

INSTALAÇÃO (uma vez):
  cd C:\Apps Python\Repos\Python\Dashboard
  python -m pip install -r requirements.txt
  python -c "import pwa_client; pwa_client.start_device_flow()"  ← login inicial
  Abrir PowerShell COMO ADMINISTRADOR
  .\setup_scheduler.ps1

A partir daí, roda sozinho. O token MSAL é renovado a cada execução
(refresh token válido por 90 dias).

QUANDO PARAR DE FUNCIONAR (raro):
  Sintoma: data/last_update.json mostra erro de auth.
  Causa:   token expirou (90 dias sem atividade) ou senha mudou.
  Fix:     python -c "import pwa_client; pwa_client.start_device_flow()"
           (nao existe reauth.py — o device flow mora no pwa_client)


================================================================================
  15. PADRÕES DE CÓDIGO QUE FUNCIONAM
================================================================================

Listar projetos com custom fields (batch de 15, paralelo):
  url = (
    PS_BASE + "/Projects"
    "?$filter=" + " or ".join(f"Id eq guid'{pid}'" for pid in batch)
    "&$expand=ProjectSummaryTask,Draft/IncludeCustomFields"
    "&$select=Id,Name,StartDate,FinishDate,PercentComplete,"
            "ProjectSummaryTask/BaselineStart,..."
            "Draft/IncludeCustomFields"
  )

Buscar tarefas de um projeto (1 request):
  url = (
    PS_BASE + "/Projects"
    "?$filter=Id eq guid'<PID>'"
    "&$expand=Tasks,Tasks/Assignments,Tasks/Assignments/Resource"
  )

Headers:
  Authorization:  Bearer <token>
  Accept:         application/json;odata=verbose
  Content-Type:   application/json;odata=verbose


================================================================================
  16. DATAS NULAS NO MSP
================================================================================

O Project Server usa "0001-01-01T00:00:00" para representar "sem data".
Tem que tratar explicitamente como None (vide _parse_date em pwa_client.py).


================================================================================
  17. ENDPOINTS DE DIAGNÓSTICO ÚTEIS
================================================================================

GET /_api/web/currentuser?$select=LoginName,Email,Title
    → confirma usuário autenticado

GET /_api/web/currentuser/groups
    → lista grupos SharePoint do usuário

GET /_api/web/sitegroups
    → lista todos os grupos do site PWA

GET /_api/ProjectServer
    → metadata do servidor, lista coleções acessíveis

GET /_api/ProjectServer/UserPermissions
    → permissões PWA reconhecidas pelo servidor para o usuário


================================================================================
  18. REFERÊNCIAS
================================================================================

MSAL Python:        https://github.com/AzureAD/microsoft-authentication-library-for-python
PWA REST API:       https://learn.microsoft.com/en-us/previous-versions/office/project-2016-pwa-rest-api
Project CSOM:       Microsoft.ProjectServer.Client (DLL)
OData v3 (usado):   https://www.odata.org/documentation/odata-version-3-0/


================================================================================
  18.1 BARRA LATERAL (SIDEBAR) COLAPSÁVEL
================================================================================

A sidebar esquerda (.sidebar / #sidebar) agora pode ser recolhida/expandida:
  - Botão circular "‹/›" (id sidebar-toggle) ancorado na borda direita da
    sidebar, alterna a classe "collapsed" no elemento #sidebar
  - Estado expandido: largura var(--sidebar-w) = 220px (rótulos visíveis)
  - Estado colapsado: largura var(--sidebar-w-collapsed) = 60px
    (mostra somente os ícones, centralizados; oculta brand, nav-section,
    nav-label e sidebar-footer via "display:none")
  - .main desloca margin-left de acordo (sibling selector
    ".sidebar.collapsed ~ .main"), com transição suave (.2s ease)
  - Preferência persistida em localStorage (chave "pmo-sidebar-collapsed")
    e restaurada no carregamento da página via initSidebar()
  - Cada item de navegação agora tem o texto envolvido em
    <span class="nav-label"> (antes era texto solto após o ícone) — isso
    é o que permite escondê-lo via CSS quando colapsado

Função JS: toggleSidebar() — alterna a classe e salva no localStorage.


================================================================================
  19. ABA "POR PROJETO" — GANTT (estado atual da UI)
================================================================================

CABEÇALHO DO SELETOR DE PROJETO (proj-selector-meta)
  Campos exibidos: Status (badge) | Cliente | Nº Horizontes | Coordenador |
  Cidade | Início | Término | Duração
  REMOVIDOS do cabeçalho: % Concluída, % Previsto e Aderência.

  IMPORTANTE — origem de Início/Término/Duração:
  NÃO vem de proj.blStart/blEnd nem de baseline — são as datas/duração
  ATUAIS (Start/Finish, refletindo reprogramações). Vêm da TAREFA-RESUMO
  DO PROJETO (Nível de Estrutura de Tópicos / OutlineLevel = 0, task_type
  "project"), retornada por /api/tasks/<id> junto com as demais tarefas.
    Início   → t.start   (NÃO t.blStart)
    Término  → t.end     (NÃO t.blEnd)
    Duração  → t.days (dias corridos/calendário entre start e end);
               fallback: round((end - start) / 86400000) se "days" ausente
  Exibida como "<n> dias".
  Por isso o cabeçalho mostra "—" inicialmente (placeholders com id
  proj-meta-inicio-plan / proj-meta-termino-plan / proj-meta-duracao-plan)
  e só é preenchido depois que o fetch de /api/tasks/<id> retorna e
  renderGantt(tasks) roda — busca-se o item com level === 0
  (ou type === 'project') na lista de tasks.
  Datas formatadas via fmtDate() local (dd/mm/aaaa).

ESCALA HORIZONTAL DINÂMICA
  PX_POR_DIA = availableWidth / totalDays
  A barra do projeto sempre cabe inteira na tela sem scroll horizontal.
  totalPx = availW (valor fixo, NÃO flex:1)

SCROLL HORIZONTAL (correção aplicada)
  .gantt-right-cell e .gantt-header-right NÃO usam flex:1 nem overflow:hidden.
  Ambos recebem width:${totalPx}px fixo — elimina área em branco ao rolar à direita.

BARRA DE PROJETO (OutlineLevel = 0)
  Cor de fundo: #4f46e5 (azul índigo)
  Altura: 20px
  Texto: preto (#1e293b) dentro da barra

BARRAS DE TAREFA RESUMO (OutlineLevel 1–3)
  Forma П: border-top 2px + border-left 2px + border-right 2px, cor #1e293b
  Fundo: transparente
  Altura: 14px
  Texto: nome da tarefa alinhado à esquerda, preto (#1e293b), DENTRO da barra
  SEM exibição de datas externas à barra

BARRAS DE TAREFA DETALHE (folha, não-summary)
  Altura: 14px  |  top: 4px  |  border-radius: 2px
  Regra de cor (em ordem de prioridade):
    1. PercentComplete >= 100  → verde   #22c55e  (concluída)
    2. Nome contém "Análise"   → cinza   #94a3b8
    3. IsCritical = true       → laranja #f97316
    4. Demais                  → azul    #60a5fa
  Texto: preto (#1e293b) dentro da barra

TABELA (coluna esquerda do Gantt)
  Altura das linhas: 22px (reduzida de 30px)
  Altura do header:  26px
  Fontes: todas em #1e293b (preto)
  Coluna "% Concluído": mantém cores verde/amarelo/vermelho via adhColor()
  Formato de datas: dd/mm/aa
  Duração: valor numérico + sufixo " dias"


================================================================================
  20. ABA "POR CIDADE" — MAPA LEAFLET (estado atual da UI)
================================================================================

CITY_COORDS (coordenadas cadastradas):
  Belo Horizonte    → (-19.9167, -43.9345)
  São Paulo         → (-23.5505, -46.6333)
  Brumadinho        → (-20.1417, -44.2017)
  Ourilândia (PA)   → (-6.7603,  -51.0806)  ← região Vale/Carajás
  Poços de Caldas   → (-21.7883, -46.5611)
  Mário Campos      → (ver código — ainda não cadastrado)
  + 20 capitais adicionadas para expansão futura

COMPORTAMENTO DO MAPA
  fitBounds automático: o mapa sempre enquadra TODAS as cidades com projetos.
  Círculos: cor única #4f46e5 (removida codificação por aderência).
  Tamanho proporcional ao nº de projetos: radius = 8 + (count/maxCount) * 30
  console.warn emitido para cidades sem coordenada cadastrada.


================================================================================
  21. ABA "VISÃO GERAL" — TABELA DE ADERÊNCIA (estado atual da UI)
================================================================================

Colunas da tabela "Aderência por Projeto" (em ordem):
  Projeto | Coordenador | Início | Término | Dur. Plan. | Últ. Publicação | Status | % Previsto | % Concluído | Aderência

"Últ. Publicação" exibe p.ultimaPublicacao via fmtDate().
Requer rodar o fetcher para que o campo seja preenchido no projects.json.


================================================================================
  22. CAMPOS ADICIONADOS AO pwa_client.py (atualizações recentes)
================================================================================

Em fetch_tasks():
  "critical": bool(t.get("IsCritical", False))
  → adicionado a cada objeto de tarefa retornado

  *** DESCOBERTA IMPORTANTE (08/06/2026): a tarefa-resumo do projeto
  (Nível de Estrutura de Tópicos / OutlineLevel = 0) NÃO vem dentro da
  coleção $expand=Tasks — essa coleção começa em OutlineLevel = 1.
  A tarefa nível 0 é uma SUB-ENTIDADE PRÓPRIA do projeto chamada
  "ProjectSummaryTask" (mesma usada em fetch_projects para % concluído
  e baseline). Ela tem Start/Finish/BaselineStart/BaselineFinish/
  PercentComplete/IsCritical igual a uma task normal.

  CORREÇÃO: fetch_tasks() agora inclui ProjectSummaryTask no $expand
  ("$expand=Tasks,Tasks/Assignments,Tasks/Assignments/Resource,
  ProjectSummaryTask") e INJETA manualmente um registro sintético com
  level=0 / type="project" / outlineLevel=0 como primeiro item da lista
  retornada — usando os campos de ProjectSummaryTask (Start, Finish,
  BaselineStart, BaselineFinish, PercentComplete, IsCritical).
  O campo "days"/"diasCorridos" é calculado via _calendar_days(start, end).
  Sem essa injeção, o frontend nunca encontra t.level === 0 e os campos
  Início/Término/Duração do cabeçalho da aba "Por Projeto" ficam em "—".

Em fetch_projects():
  "ultimaPublicacao": valor de LastPublishedDate (parseado via _parse_date)
  → adicionado ao $select da chamada à API
  → armazenado como string "YYYY-MM-DD" no projects.json

  *** CORREÇÃO (08/06/2026): o campo correto na entidade PS.PublishedProject
  é "LastPublishedDate" — NÃO "ProjectLastPublishedDate". O nome errado fazia
  o $select ignorar o campo silenciosamente (sem erro HTTP), retornando
  sempre None/"—" no frontend. Confirmado via:
    GET /_api/ProjectServer/Projects?$top=1
    → campos de data disponíveis: CheckedOutDate, CreatedDate,
      LastPublishedDate, LastSavedDate, CurrentDate, FinishDate,
      StartDate, StatusDate, UtilizationDate

================================================================================
  FIM
================================================================================


================================================================================
  16. BASE DO REPORT E COMPARADOR DE CRONOGRAMAS
================================================================================

A REGRA QUE DEFINE TUDO
-----------------------
O comparativo tem exatamente DOIS lados: o cronograma de agora e o cronograma de
quando o ultimo report foi gerado. Nao existe terceira data. Publicacao de
cronograma que nao virou report nao e comparada com nada e nao e guardada.

Disso decorre que nao ha historico a manter: ha UM estado por projeto,
substituido a cada report gerado.

  * Houve um historico.py com 15 versoes em "base completa + deltas", retencao
    com dobra do delta antigo na base, reconstrucao por data (versao_em) e
    hierarquia por parentId para nao reescrever indices. Tudo aquilo existia
    para responder "como era o cronograma numa data qualquer" — pergunta que o
    report nao faz. Foi apagado junto com os data/history_*.json.

BASE DO REPORT (report_base.py)
-------------------------------
data/report_base_<pid>.json, gravado por app.py ao fim de cada geracao de
report. Guarda TRES campos por tarefa: id, start, end.

  - Sao os unicos que o lado antigo da comparacao e lido para dar: o pareamento
    e por id e a variacao e de datas. Nome, recurso, nivel e hierarquia saem
    sempre do cronograma ATUAL, que e quem escreve o rotulo de cada linha.
  - A ordem da lista importa: o primeiro item e a tarefa-resumo do projeto
    (nivel 0), de onde saem o termino e a duracao totais.
  - `publicadoEm` (LastPublishedDate COM hora) carimba a base e vira a data do
    titulo da secao. `geradoEm` e o instante da geracao, usado so como fallback.
  - API inteira: carregar(), gravar(), data(). Tres funcoes.

  GRAVAR E DEPOIS DE MONTAR O RELATORIO, nunca antes: base gravada num report
  que falhou ao ser montado engoliria o periodo seguinte.

  Gerar o report duas vezes seguidas faz o segundo comparar o cronograma consigo
  mesmo e dizer que nada mudou. E consequencia direta da regra, e esta acordado:
  sai um report por dia.

  SEMEADURA (feita uma vez, em 13/08/26): as 16 bases nasceram da versao mais
  antiga retida nos history_*.json antes de eles serem apagados, para que o
  primeiro report do sistema novo ja tivesse periodo para comparar. Projeto novo
  que apareca daqui em diante nao tem base: o primeiro report dele sai sem a
  secao, com aviso, e grava a base para o proximo.

COMPARADOR (comparador/)
------------------------
Os dois lados sao data/report_base_<pid>.json e data/tasks_<pid>.json. Ambos tem
GUID de tarefa, entao o pareamento e por id.

  comparador.secao_desde_base(base, tarefas_atuais) -> markdown

Renomear tarefa NAO quebra a comparacao. Conferido com as "Analise" do Vila
Gilda (48) e da Brasilandia (52) renomeadas para "Analise 1..N" so no lado
atual: 0 removidas, 0 inseridas, e a secao sai com os nomes novos. O pareamento
e por GUID; o nome so e lido para escrever o rotulo, e vem sempre do cronograma
ATUAL. Importa porque as Analises estao sendo renomeadas aos poucos.

COMO O COMPARATIVO CHEGA NA TELA
--------------------------------
    index.html (form-report)
      -> POST /api/report-json {project_id, nome_projeto}
           app._secao_comparativa()      -> report_base.carregar()
                                         -> comparador.secao_desde_base()
           Report.gerar_relatorio_web_json(tarefas, nome, secao)
           report_base.gravar()          <- o cronograma de agora vira a base
                                            do proximo report
      <- {content, filename, comparativo:{desde | aviso}}

  Nao ha seletor de data na tela nem parametro de data na rota: nao existe o que
  escolher, ha uma base so. Houve um seletor "Comparar com" e um GET
  /api/versoes/<pid> enquanto o modelo era de historico; sairam junto com ele.

  Comparativo e acessorio: se ele falhar, o report sai sem a secao e o aviso vai
  em `comparativo.aviso` — nunca deixa o usuario sem relatorio.

POR QUE NAO HA CALCULO DE CAMINHO CRITICO
-----------------------------------------
Medido nos 17 cronogramas: as tarefas-folha criticas cobrem 3% a 17% do prazo na
maioria dos casos (Vila Gilda 8%, Brasilandia 3%); a UBS Santa Rita nao tem
nenhuma; e andar do termino para tras pelos vinculos percorre a cadeia inteira
em apenas 7 dos 17 projetos.

O motivo e que esses cronogramas sao ancorados por data contratual: o marco
final e fixo, entao quase tudo antes dele tem folga. Atribuir dias "ao longo da
cadeia critica" produziria numeros que parecem precisos e nao sao.

O QUE FUNCIONA: A CADEIA DE VINCULOS
------------------------------------
A cobertura de vinculos e alta (89-100% das folhas tem predecessora) e a
aderencia tambem (86-100% dos FS coerentes com as datas). rede.py caminha do
termino do projeto para tras, seguindo em cada passo a predecessora cujo
deslocamento explica o da sucessora (empurrador()), e coleta quem AUMENTOU A
DURACAO no caminho.

Ofensor e quem aumentou a duracao dentro dessa cadeia. Tarefa que so foi
deslocada por inteiro herdou o atraso, nao o produziu. Marco (duracao zero)
nunca e ofensor: empurrador() pula por cima dele para apontar a causa real.

Tolerancia: datas sao de calendario e vinculos sao em dias uteis, entao o
casamento de deslocamentos usa folga de 3 dias (rede.TOL_DIAS).

DOIS PONTOS EM QUE A CAMINHADA MORRIA (corrigidos em 14/08/26)
--------------------------------------------------------------
1) Vinculo FS descartado por incoerencia de data. A checagem de _respeitado()
   agora so vale em tarefa NAO INICIADA: depois que comeca, a data e fato e
   entrar com inicio real antes do fim da predecessora e sobreposicao de obra.
   Nos 16 cronogramas, as 223 ligacoes incoerentes tinham TODAS a sucessora ja
   iniciada — descartar todas cegava a analise sem motivo.

2) Vinculo apontando para tarefa-resumo. Resumo fica fora dos deltas de
   proposito (espelha as filhas e contaria duas vezes), entao a caminhada
   morria nele. Agora rede._condutora() resolve o resumo para a filha que
   termina junto com ele — a que manda no termino — e a caminhada segue.

   No Pirajucara, o caso que originou a analise: a cadeia foi de 2 para 3 elos
   e os ofensores passaram a somar 6 de 6 dias, contra 1 de 6 antes.

O que NAO se resolve por codigo: tarefa que anda sem que nenhuma predecessora
ande (restricao de data, nivelamento, remarcacao manual). A causa nao esta na
rede. O Palhano, ja com os vinculos limpos, explica 3 dos 8 dias por isso.

O BLOCO 📌 RESUMO
-----------------
Quatro linhas, montadas por Report._bloco_resumo(). A funcao nasceu para servir
dois caminhos (upload de Excel e JSON do PWA); desde 17/08/26 o Excel foi
removido e o report so sai do snapshot do PWA.

    - Conclusao do Projeto - Linha de Base: 19/06/25.
    - Conclusao do Projeto - Tendencia: 08/09/26.
    - Duracao do Projeto - Linha de Base: 310 dias corridos.
    - Duracao do Projeto - Tendencia: 757 dias corridos.

Linhas pareadas: linha de base e tendencia, primeiro a conclusao e depois a
duracao. Quem le compara os dois numeros lado a lado — desde 14/08/26 o desvio em
dias e o percentual de variacao nao vem mais escritos (versao anterior trazia os
dois). Duracao da tendencia leva +1 (o dia de inicio conta); a da linha de base
nao — assimetria antiga, mantida para os numeros nao mudarem.

A SECAO DO REPORT SEMANAL
-------------------------
Entra logo depois do resumo, via Report._montar_relatorio_md(secao_comparativo).

    🤔 O QUE MUDOU DESDE O ULTIMO REPORT(07/08/26)?
    - A Previsao de Conclusao mudou de ... para ..., gerando um atraso de N ...
    - A Duracao total do projeto mudou de ... para ..., gerando um aumento ...

    🚨PRINCIPAIS OFENSORES
    - <bisavo - avo - pai - nome>: Aumento na duracao de +N dias corridos
      (Responsavel: X, Termino atual: dd/mm/aa).

Cabecalho e emoji + caixa alta, como as demais secoes: o report e lido como
texto puro (colado em e-mail/WhatsApp), nao renderizado como markdown — "##" e
"**" apareciam crus. Sem linha em branco entre o cabecalho e o primeiro item.

Ate 3 tarefas, da que mais esticou para a que menos esticou.

O numero da linha e o AUMENTO DE DURACAO, nao a variacao do termino: parte do
deslocamento e herdada da predecessora, e so o que a tarefa acrescentou de
duracao e responsabilidade dela. Por isso a linha traz o termino ATUAL sozinho,
sem o par "de -> para": o par sugeria que a diferenca entre as duas datas e o
numero da linha, e nao e — ali cabe o deslocamento inteiro, heranca inclusa. No
Vila Gilda a segunda linha mostra o caso: termino de 25/09 para 05/10 (10 dias
de deslocamento) com aumento de duracao de apenas +2.

LISTA RETIRADA: MAIORES VARIACOES
---------------------------------
Havia uma segunda lista, "Maiores Variacoes (Tarefas com slack positivo)", com
as tarefas de maior variacao FORA da cadeia — variaram sem mover o prazo, logo
tinham folga. As duas listas eram mutuamente exclusivas, o que tornava o rotulo
"slack positivo" verdadeiro sem depender do TotalSlack do servidor (que volta
zerado ate em tarefas com IsCritical=False).

Saiu do report: respondia a uma pergunta que o report semanal nao faz. Quem le
quer saber o que move a entrega, e a lista competia por atencao com a que
responde isso.

Ficou dentro do modulo o que a sustentava: `comparar()` ainda calcula a variacao
de cada folha pareada, porque a rede precisa desses deltas para achar o
empurrador. O que saiu foi a ordenacao por variacao e a lista no markdown.

MARCOS FORA DA ANALISE
----------------------
Tarefa de duracao zero (`marco`, de DurationMilliseconds == 0) nao entra na
lista: nao tem duracao para esticar nem recurso a quem atribuir. Conferido
contra o IsMilestone do servidor: 94 de 94 coincidem em 1.018 tarefas. Deduzir
por start == end teria errado — existe tarefa de 1 dia com inicio e termino no
mesmo dia que nao e marco.

GRAFIA
------
Rotulo = bisavo - avo - pai - nome, a convencao do report_semanal/Report.py. A
busca dos ancestrais e relativa (os tres mais proximos), nao por nivel fixo:
mesmo resultado nas tarefas de nivel 4 (a maioria) e correto nas mais rasas,
onde o nivel fixo pegaria um ramo alheio. A linha de nivel 0 (o projeto) nunca
entra como ancestral. Recurso aparece como "Responsavel: <nome>".


================================================================================
  17. ANALISE DE SAUDE DOS CRONOGRAMAS
================================================================================

Mede a qualidade ESTRUTURAL da rede de dependencias — o que faz, ou impede, a
analise de ofensores do report semanal funcionar. Todo KPI aqui nasceu de um
defeito observado: o Pirajucara perdeu 5 dos 6 dias de atraso porque a caminhada
morreu numa tarefa-resumo.

Motor: saude/saude.py. Rotas: GET /api/saude/<id> (completo) e GET /api/saude
(todos os projetos, com as notas por indicador; o mestre consolidado fica fora).

DUAS TELAS
----------
  view-saude      Painel geral, e o que abre no menu. Medidor da media da
                  empresa, termometro da distribuicao por faixa, 6 cards de
                  destaque (3 melhores / 3 piores) com observacao lida do
                  proprio dado, radar comparando os dois grupos por indicador,
                  barra de concentracao dos defeitos e mapa de calor
                  projetos x indicadores. Tudo clicavel.
  view-saudeproj  Analise de um projeto: seletor, medidor, diagnostico em texto,
                  os 6 cards de KPI com o porque e a acao corretiva, e a lista
                  de apontamentos (abaixo).
                  Nao tem item de menu — NAV_ALIAS mantem "Saude" aceso.

LISTA DE APONTAMENTOS (18/08/26)
--------------------------------
O KPI diz quantas tarefas estao erradas; a lista diz QUAIS SAO e ONDE ESTAO.
Sem ela o coordenador tem a nota e nao tem como agir: achar as 49 tarefas com
dependencia em resumo no meio das 221 linhas do Pirajucara e trabalho manual.

Cada cartao de KPI com defeito e clicavel e abre a lista ja filtrada por ele;
as abas trocam o filtro; o cabecalho da tabela ordena (clique de novo inverte,
com a linha como desempate). Colunas: linha, tarefa + caminho na EAP, indicador,
motivo especifico, datas e recurso.

  - LINHA e a posicao da tarefa no snapshot, contada a partir de 1. O snapshot
    nao guarda o Id nem a WBS do Project, entao ela bate com a linha do Project
    quando o cronograma esta sem filtro nem reordenacao. E o que a dica no pe da
    tabela explica (Ctrl+G leva ate a linha; Ctrl+L acha pelo nome).
  - CAMINHO NA EAP faz o trabalho pesado de identificacao: um cronograma tem
    dezenas de tarefas chamadas "Analise" e "R00", e o nome sozinho nao diz qual
    e qual. Vem de _caminhos(), que reconstroi o caminho com uma pilha indexada
    pelo nivel — a hierarquia do MSP e posicional.
  - MOTIVO e por tarefa, nao a definicao do KPI: "predecessora e o resumo
    'Implantacao' (linha 15)", "ninguem depende dela: termina em 02/06/2025 e o
    atraso morre ai".

No modulo: _medir() ja montava os conjuntos de ids ofensores e jogava fora no
fim, ficando so com len(). Agora devolve tambem `ofensores` (id -> motivo), e
_itens() os transforma em linhas. analisar(detalhar=False) desliga isso — e o
que resumo() usa, porque o painel geral analisa a carteira inteira so para
desenhar o mapa de calor e nao abre lista nenhuma. Nenhuma nota mudou.

Sem `itens` na resposta (servidor anterior a esta tela) o card da lista some e
os KPIs voltam a ser os de antes, sem link para uma lista que nao veio.

O radar deixa de fora indicador sem nota em nenhum projeto: eixo em zero seria
lido como nota zero, e nao como ausencia de dado.

O SCORE E UMA TAXA, NAO UMA CONTAGEM
------------------------------------
nota = 100 * (1 - taxa/limite), com taxa = tarefas com o defeito / tarefas-folha.
Sem isso o Sede 2 (2.286 folhas) seria sempre pior que o Jardim Coreano (19), o
que diria mais sobre o tamanho do projeto do que sobre a qualidade dele. O score
final e a media das notas ponderada pelos pesos.

  KPI                              peso  limite   o que conta
  Dependencias ligadas a resumo      30    10%    pred e resumo, ou resumo com pred
  Tarefas sem sucessora              20    25%    tarefa que nao empurra ninguem
  Tarefas sem predecessora           15    25%    tarefa sem nada antes
  Tarefas nivel 4 sem recurso        10    10%    nivel 4, fora marcos, sem recurso
  Marcos com duracao                 10     5%    IsMilestone com duracao > 0
  Trabalho fora do nivel 4           15    30%    tarefa de trabalho em nivel != 4

Faixas: 90 Excelente · 75 Bom · 60 Regular · 40 Ruim · abaixo disso Critico.

DESCONTOS DELIBERADOS
---------------------
  - A primeira tarefa nao precisa de predecessora; quem termina junto com o
    projeto nao precisa de sucessora. Ambas saem da conta.
KPIS RETIRADOS
--------------
Media a sucessora que comeca antes do fim da predecessora. Saiu em 14/08/26: das
223 ocorrencias da base, 223 tinham a sucessora ja iniciada (pct > 0) — ou seja,
eram sobreposicao real de obra, com a data virando fato, e nao erro de logica.
O indicador dava nota cheia para todo mundo e nao media nada. No lugar entrou
"tarefas sem linha de base", que discrimina de 0% (Sede 2) a 100% (Rota Roxa) e
tem efeito direto: o bloco RESUMO do report sai de blStart/blEnd.

"Tarefas sem linha de base" tambem saiu, no mesmo dia, por decisao do usuario.
Os 15 pontos dele voltaram para marcos (5) e nivel 4 (5), que eu tinha cortado
para abrir espaco ao KPI de recurso, e os 5 restantes foram para dependencias em
resumo, que e o que de fato trava a analise.

MARCO: DUAS COISAS DIFERENTES
-----------------------------
`marco` = duracao zero (derivado). `isMilestone` = o flag do Project, a INTENCAO
de ser marco. O defeito e a divergencia: marcado como marco, mas com duracao.
O flag entrou no snapshot em 14/08/26 (pwa_client.py); em snapshot anterior o
KPI sai como indisponivel e os pesos sao renormalizados, em vez de mentir 100.
